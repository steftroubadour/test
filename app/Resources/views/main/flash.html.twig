{% if app.request.hasPreviousSession %}
    {% for type, messages in app.session.flashbag.all() %}
        {% for message in messages %}
            <div class="flash-{{ type }} alert-{{ type }} text-center mx-auto">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
{% endif %}
